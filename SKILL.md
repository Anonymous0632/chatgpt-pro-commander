---
name: chatgpt-pro-commander
description: 当 ChatGPT Pro 指挥工作流已全局启用，或用户显式调用 $chatgpt-pro-commander 时，使用已登录的 ChatGPT 网页 Chat 界面中经过验证的精确 Pro 模式负责新任务的规划、决策与最终审查，并由 Codex 独立执行本地操作和真实测试。不要用于启用、暂停、查询本工作流状态，用户明确要求当前任务跳过 Pro，或继续一个已经绑定 task_id 的同一任务。
---

# ChatGPT Pro 指挥者

把一个新的用户目标交给 ChatGPT 网页版的精确 `Pro` 模式规划，再由 Codex 执行，并让同一 Pro 对真实结果作最终审查。Pro 是技术指挥者；Codex 是唯一的本地写入者、命令执行者和测试运行者。

## 不可替代的边界

- 只接受 ChatGPT 官方网页 **Chat/聊天** 界面中，语义化模型控件明确选中的 `Pro` 模式。账户套餐徽标、页面正文中的 `Pro`、普通模型、Work/工作模式、API 模型或 Codex 自身推理都不能替代。
- 使用网页控制发送消息时，必须遵守所选浏览器控制面的现行确认要求。任何发送前确认都由用户本人完成；脚本、扩展、CDP、坐标点击或页面注入不能代替确认。
- 不读取或复用 Cookie、浏览器存储、登录令牌、密码、验证码或会话数据库。
- Pro 的建议不扩大用户授权。提交、推送、PR、部署、发布、数据库迁移、线上配置和真实用户数据操作仍需用户在当前任务中明确授权。
- Pro 不能声称已经读取一个本地路径、修改本地文件或运行本地测试。只有实际上传的附件和 Codex 记录的真实执行证据有效。
- Pro 无法确认、附件无法证明已送达、回复不完整或协议标记不匹配时，停止并如实报告；禁止静默换用其他模型。

## 读取指南

每个新任务都读取 [任务协议](references/task-protocol.md)。

- 在首次操作 ChatGPT 网页前，读取 [模型与浏览器门禁](references/model-and-browser-gates.md)。
- 需要向 Pro 提供本地文件、源码、日志或测试证据时，读取 [附件与安全规范](references/attachment-policy.md)。
- 遇到发送不确定、长时间生成、页面中断或需要最终报告时，读取 [恢复与报告](references/recovery-and-reporting.md)。
- 安装、启停、全局默认调用和示例见 [使用说明](references/usage.zh-CN.md)。

## 角色分工

### ChatGPT Pro

- 定义问题、成功标准、实施计划、取舍、风险和测试判定标准。
- 基于实际收到的附件和执行证据给出 `PLAN` 或 `REVIEW`。
- 在 REVIEW 中明确返回 `PASS`、`REVISE` 或 `BLOCKED`。
- 提供简洁的依据摘要、假设、证据权重和最强反方意见；不要请求或输出隐藏思维链。

### Codex

- 读取仓库规则，确认基线、权限和可执行环境。
- 选择最小但充分的证据，生成安全附件并运行凭据扫描。
- 验证 Pro 计划没有越权，且关键事实与源码、命令输出一致。
- 只执行当前用户授权范围内的本地修改和命令，记录原始测试结果。
- 当 Pro 判断与可复现事实冲突时，把具体证据退回同一会话重新判断；不以主观偏好取代 Pro。

## 标准流程

1. **模式检查**：全局自动调用时先确认工作流为启用状态。显式 `$chatgpt-pro-commander` 即使处于暂停状态也可单次运行。
2. **本地预检**：读取适用的 `AGENTS.md`、项目说明、依赖清单、Git 分支和工作树状态。保留现有改动。
3. **任务建模**：生成一个 UUID `task_id`，写清目标、验收标准、范围、禁止操作、当前事实、未知项和需要 Pro 决定的问题。
4. **证据准备**：按附件规范生成源码 ZIP、Markdown Bundle、安全报告和 Manifest；记录 commit、分支、状态、大小与 SHA-256。
5. **网页预检**：按用户明确选择的浏览器控制面操作；用户未指定时使用运行时默认控制面。确认登录、Chat/聊天界面和精确 Pro 模式。
6. **PLAN**：建立新的 ChatGPT 会话，准备协议请求和附件，在发送前取得所选控制面要求的当次确认，只发送一次并等待完整回复。
7. **计划门禁**：校验任务 ID、消息 ID、阶段和唯一回复标记。验证范围、权限和事实后进入执行；不合格则向同一 Pro 会话提供最小纠错证据。
8. **执行**：Codex 进行本地修改和测试。保留失败输出，不把模拟、静态检查或构建成功冒充为真实设备、生产或线上验证。
9. **REVIEW**：生成包含实际 diff、测试结果和未验证项的复审附件；再次执行模型门禁和发送确认，让同一 Pro 检查结果。
10. **闭环**：`PASS` 后进行一次本地事实复核；第一次 `REVISE` 可执行一次修正并再次 REVIEW；第二次仍非 `PASS` 时停止为 `BLOCKED`。
11. **交付**：按报告规范区分计划、实际修改、测试、Pro 结论、风险，以及是否提交、推送、部署或发布。

## 证据门禁

Codex 可以拒绝执行或把问题退回 Pro，但理由仅限：

- 超出用户授权或产品范围；
- 违反安全、隐私、仓库规则或不可逆操作边界；
- 依赖不存在的文件、工具、权限或环境；
- 与可定位源码、命令输出、文件哈希或真实测试结果矛盾；
- 无法满足明确验收标准。

普通实现偏好由 Pro 决定。若门禁触发，必须给出文件位置、错误文本、测试结果或权限边界等具体证据。

## 停止条件

- 登录、账号选择、验证码、Passkey 或两步验证需要用户接管。
- 精确 Pro 不可见或无法语义化确认时为 `BLOCKED`。
- 发送状态为 `SENT` 或 `UNKNOWN` 时不得重复发送；只恢复原会话。
- 缺少预期回复标记、附件未确认送达、回复截断或协议不一致时为 `ERROR`。
- 一次初始执行加一次自动修正后仍未通过时为 `BLOCKED`。
- 用户暂停、撤销授权或改变目标时立即停止当前阶段并保留证据。

## 最终输出

最终报告至少包含：

- `task_id`、最终状态和 ChatGPT 会话链接；
- 经过验证的活动模型标签、控制面和确认结果；
- 源码基线、附件名称、大小和 SHA-256；
- Pro 的 PLAN、Codex 实际执行内容和 Pro 的最终 REVIEW；
- Pro 要求修正的问题及每次修正结果；
- 实际运行的测试、通过/失败/未运行状态和未验证风险；
- 当前成果只是本地文件，还是已经提交、推送、创建 PR、部署或发布。

